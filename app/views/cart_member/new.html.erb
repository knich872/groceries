<%= simple_form_for @cart_member do |f| %>
  <div class="form-inputs">
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :shopping_cart_id, @shopping_cart.id %>
  </div>
  <div class="form-actions">
    <%= f.button :submit, "Join" %>
  </div>
<% end %>
