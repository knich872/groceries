<%#= link_to "View this item", shopping_cart_item_path(@shopping_cart.id), class: "btn" %>
<%= item.name %>
<% item.photos.each do |photo| %>
  <%= cl_image_tag photo.key, width: 400, height: 400, crop: :fit %>
<% end %>
